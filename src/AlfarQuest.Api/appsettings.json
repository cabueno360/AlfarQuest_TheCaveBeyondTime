{
  "ConnectionStrings": {
    "MySql": "server=localhost;port=3306;database=alfar_quest;user=root;password=;"
  },
  "Kestrel": {
    "Endpoints": {
      "Http": { "Url": "http://localhost:5080" }
    }
  },

  "Auth": {
    "SessionLifetime": "12:00:00",
    "RememberMeLifetime": "30.00:00:00",
    "SignInAttemptsPerWindow": 8,
    "SignInWindow": "00:05:00"
  },

  "//AllowedOrigins": [
    "Ignored while the environment is Development, where the client's port changes",
    "between runs. In production, list the exact origins allowed to call this API —",
    "with bearer tokens an open policy lets any page a player visits spend their",
    "session. Leaving this empty in production falls back to open, so set it.",
    "Example: [ \"https://alfarquest.example\" ]"
  ],
  "AllowedOrigins": [],

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
